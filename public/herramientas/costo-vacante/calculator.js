const $=id=>document.getElementById(id);const money=n=>new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN',maximumFractionDigits:0}).format(Math.max(0,n||0));function calc(){const salary=+$('salary').value||0,days=+$('days').value||0,mult=+$('multiplier').value||1,hours=+$('hours').value||0,hv=+$('hourValue').value||0,months=days/30;const productivity=salary*mult*months,team=hours*hv*months,total=productivity+team;$('productivity').textContent=money(productivity);$('team').textContent=money(team);$('total').textContent=money(total);$('daily').textContent=money(days?total/days:0)}['salary','days','multiplier','hours','hourValue'].forEach(id=>$(id).addEventListener('input',calc));calc();